# Google OAuth client ID for "Continue with Google" (public client; used in the browser).
# Create credentials in Google Cloud Console and add authorized JavaScript origins for your dev URL.
VITE_GOOGLE_CLIENT_ID=

# Google Maps Embed API key for the property location map on the property detail page.
# Enable the "Maps Embed API" in Google Cloud Console → APIs & Services.
# If left empty, an OpenStreetMap embed is shown instead (free, no key required).
VITE_GOOGLE_MAPS_API_KEY=
